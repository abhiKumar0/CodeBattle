spring:
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 2MB
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/codebattle}
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
      validation-timeout: 5000
      keepalive-time: 30000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

  data:
    redis:
      url: ${REDIS_URL:redis://localhost:6379}

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_USER:}
    password: ${GMAIL_APP_PASSWORD:}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  config:
    import: optional:file:.env[.properties]

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:google-client-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:google-client-secret}
            redirect-uri: "{baseUrl}/login/oauth2/code/google"
            scope:
              - email
              - profile
          github:
            client-id: ${GITHUB_CLIENT_ID:github-client-id}
            client-secret: ${GITHUB_CLIENT_SECRET:github-client-secret}
            redirect-uri: "{baseUrl}/login/oauth2/code/github"
            scope:
              - read:user
              - user:email

server:
  port: ${PORT:8080}

app:
  frontend-url: ${FRONTEND_URL:http://localhost:3000}

judge0:
<<<<<<< HEAD
  url: ${JUDGE0_URL:http://localhost:2358}
  api-key: ${JUDGE0_API_KEY:}

jdoodle:
  client-id: ${JDOODLE_CLIENT_ID:}
  client-secret: ${JDOODLE_CLIENT_SECRET:}
=======
  url: http://localhost:2358
  api-key: ${JUDGE0_API_KEY:dummy-key}

jdoodle:
  client-id: ${JDOODLE_CLIENT_ID:dummy-id}
  client-secret: ${JDOODLE_CLIENT_SECRET:dummy-secret}
>>>>>>> dev
#  client-id: "1223"
#  client-secret: "asjsbd"


jwt:
<<<<<<< HEAD
  secret: ${JWT_SECRET:defaultSecretKeyForTestingAndStartupWhichIsAtLeast32BytesLong!}
  expiration: 86400000

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:}
  api-key: ${CLOUDINARY_API_KEY:}
  api-secret: ${CLOUDINARY_API_SECRET:}
=======
  secret: ${JWT_SECRET:dummy-secret-value-must-be-long-enough-for-hs256-signature}
  expiration: 86400000

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dummy}
  api-key: ${CLOUDINARY_API_KEY:dummy}
  api-secret: ${CLOUDINARY_API_SECRET:dummy}
>>>>>>> dev


logging:
  level:
    com.codebattle: DEBUG
#    org.springframework.web: DEBUG